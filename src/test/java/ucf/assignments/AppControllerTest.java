package ucf.assignments;

import static org.junit.jupiter.api.Assertions.*;

class AppControllerTest {

    // newList() test if it adds the name of new list

    // newTask() test if it adds new task name to list

    // getList() test if it finds list name and stores

    // getTask() test if it selects task chosen from the specific list

    // printTask() test if displays tast name from a list

    // RemoveList() test if deletes

    // RemoveTask() test if deletes task from specfic list

    // editListName() test if list name gets changed compared to old name

    // editTaskDesc() test if task description gets changed compare to previous string

    // editDueDate() test if date is changed compare to previous string

    // taskComplete() test if task gets marked as complete

    // printCompletedList() test if tasks marked as completed are printed

    // printIncompleteList() test if tasks marked as incomplete are printed

}